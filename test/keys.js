const chnageKeys = require('../index')
const expect = require('chai').expect

describe('Change keys ', () => {
    it('input object string not allowed', () => {
      let response = ''
      let model = {
            name: 'username'
       }
    })
})

